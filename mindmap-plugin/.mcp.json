{
  "_comment": "Uses ${CLAUDE_PLUGIN_ROOT} so the plugin is portable. TODO: after publishing to npm, switch to command: npx, args: [-y, mindmap-mcp-server].",
  "mcpServers": {
    "mindmap": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/../mcp-server/dist/index.js"]
    }
  }
}
